<%= simple_form_for @post do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :category, collection: Post.categories.keys %>
  <%= f.simple_fields_for @post.location do |l| %>
    <%= l.input :name %>
    <%= l.input :address %>
  <% end %>
  <%= f.input :photos, as: :attachinary %>
  <%= f.submit %>
<% end %>
