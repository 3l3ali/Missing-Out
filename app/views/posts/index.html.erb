<% @posts.each do |post| %>
  <%= link_to post.title, post_path(post) %><br>
  <%= post.description %>
  <%= link_to "Edit", edit_post_path(post) %>
  <%= link_to "Delete", post_path(post), method: :delete, data: {confirm: "Are you sure?"} %><hr>
<% end %>
