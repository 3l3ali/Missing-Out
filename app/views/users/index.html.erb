<% @users.each do |user| %>

  <% if user.photo? %>
    <%= cl_image_tag user.photo.path, width: 200, height: 300, crop: :fill %>
  <% end %>

  <%= user.first_name %>
  <%= user.last_name %>
<% end %>
